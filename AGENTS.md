# ProtoAlign 开发约束

本文件约束参与本仓库开发和维护的编码 Agent。根目录 `README.md` 是产品定义、范围、架构与开发计划的唯一权威版本；如有冲突，以 README 和用户最新明确指令为准。

## 1. 当前目标

实现一条真实纵向闭环：

```text
创建空白项目
→ 导入真实 AI 产品与文本资料
→ 采集渲染后 DOM
→ DeepSeek Agent 自主调用真实工具
→ 生成带证据的 Issue
→ 人工确认与处理
→ 导入新版本复检
```

每项任务开始前先把目标改写成可验证的成功条件。超过一步的任务先列出短计划，再实施；计划应能让用户在编码前发现方向错误。

## 2. 写代码前必须完成

- 完整阅读即将修改的文件，不凭文件名或局部片段猜测。
- 查找仓库内相似实现、现有工具函数、依赖、API 路由和测试，优先沿用已有模式。
- 检查导入和依赖，避免引入与现有方案重复的库。
- 明确影响实现方向的假设和取舍；只有高影响且无法从仓库确认的歧义才暂停询问，低风险细节采用最小合理假设并说明。
- 对不确定的外部 API、SDK 参数或版本行为，先查官方文档或实际类型定义，不编造接口。

## 3. 实现原则

### 保持简单

- 只写当前需求真正需要的最少代码，不为“以后可能需要”提前设计框架。
- 在第二个真实实现出现前，不创建只有一个实现的接口、基类、策略层或泛型抽象。
- 不增加没有现实变化需求的配置项；固定值只有在确有运行差异时才变成配置。
- 只处理实际可能发生的错误，不用大量无意义的空值检查和 `try/catch` 掩盖根因。
- 如果局部重复比错误抽象更清楚，允许暂时重复；出现稳定模式后再抽取。

### 外科式修改

- 只改与当前任务直接相关的代码，不顺手重构、改名、重新排序导入或清理无关旧代码。
- 匹配所在文件已有的命名、格式、状态管理和错误处理风格。
- 不对无关文件运行全量格式化；每一行变更都应能对应到任务或由该变更直接产生的清理。
- 修改导致的未使用导入、变量或函数应随本次改动一起清理；既有无关问题另行报告。

### 谨慎增加依赖

- 优先使用项目现有依赖和平台标准能力。
- 新增包前检查维护状态、体积、安全性和与当前技术栈的兼容性。
- 每个新增依赖都要能说明不可替代的当前用途；不得为一个简单工具函数引入大型库。

## 4. ProtoAlign 不可违反的产品与安全规则

- 不得用写死 Issue、固定成功结果或定时动画冒充 Agent Run。
- 模型、文件解析或工具调用失败时必须如实显示失败，不得静默回退到虚拟成功结果。
- Agent 只能生成草稿、建议和复检结论，不能确认事实、接受风险或关闭 Issue。
- AI 推断、原始证据和人工结论必须分字段保存，不得混合成无法追溯的摘要。
- 活动记录只保存真实工具行动、参数摘要和结果，不保存或展示隐藏思维链。
- `DEEPSEEK_API_KEY` 只能由服务端从环境变量读取；不得写入客户端、日志、测试快照、错误响应或 Git。
- 用户上传的静态网页只在受控环境中运行；首版不得执行用户提供的服务端代码。
- 未真实实现的 PDF、OCR、Figma、音频、视觉理解、自动修改和后台自主运行能力不得对外宣称。
- 删除全部预置 Issue 后，真实项目导入和 Agent 分析仍必须工作；输入变化必须导致工具结果和 Issue 随之变化。

## 5. 调试规则

- 先完整阅读错误信息和堆栈，再形成假设。
- 修改前先稳定复现问题；无法复现时报告已检查的条件，不声称已经修复。
- 一次只验证一个主要假设，避免同时改动多个位置后无法判断真正原因。
- 不用空值保护、重试或回退掩盖未知根因；先解释为什么会发生。
- 卡住时明确列出已尝试内容、实际证据和剩余不确定点，不随机尝试直到偶然通过。

## 6. 验证规则

- 修复缺陷时先写或补充能复现问题的测试，确认修复前失败、修复后通过。
- 修改前后都运行相关既有测试；若存在原有失败，先记录，不能把它归因于本次修改或静默忽略。
- 测试行为和业务结果，不测试无价值的内部实现细节。
- 根据风险选择验证层级：类型检查、单元测试、接口测试、Agent 固定样例、端到端浏览器测试和人工肉眼检查。
- Agent 相关改动至少验证：真实模型调用、真实工具事件、证据引用、结构化输出校验、失败路径和人工权限边界。
- 无法自动测试的部分必须说明原因，并给出可重复的人工验证步骤。
- 完成前检查最终 diff，确认没有秘密、调试输出、生成垃圾、无关格式化或任务外修改。

## 7. 沟通与版本控制

- 开始时说明目标、关键假设和计划；完成时说明结果、原因、测试证据和仍存在的限制。
- 对架构、数据库结构、API 形状、认证和新依赖等难以逆转的决定，不能默默选择，必须明确记录。
- 精确区分“已实现并验证”“已实现但未验证”“计划支持”和“暂不支持”。
- 提交信息描述具体行为变化，不使用“fix bug”“update”等模糊措辞。
- 提交和推送前检查暂存范围，确保 `.env.local`、数据库、上传文件和测试产物不进入 Git。
- 不因顺手清理扩大一次提交；一个提交应对应一个清楚的成果或修复。

## 8. 常见失败模式自检

发现以下迹象时应暂停并重新评估：

- 为一个功能重构半个项目；
- 只有一个实现却设计多层抽象；
- 未说明就作出数据库、接口或模型架构决定；
- 只处理成功路径，不考虑 API 失败、空输入、坏文件和超时；
- 使用未核实的 SDK 方法或参数；
- 代码风格与所在文件明显不同；
- 修复不断扩散到更多无关文件；
- 测试只证明页面能点，却不能证明 Agent 真正读取输入并调用工具。

## 9. 关键资料

- Agent 架构与使用流程：`docs/architecture/agent-architecture.md`
- 产品逻辑：`docs/product/product-logic.md`
- 当前视觉原型：`prototypes/current/detailed-workbench.html`
- 当前视觉参考：`prototypes/current/blue-black-deepened.png`

任何新确认的产品结论须在同一轮同步修订 `README.md` 对应内容及变更记录。

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
