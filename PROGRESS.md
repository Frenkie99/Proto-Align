# 隔夜真实案例验证进度

- 目标：用三个公开真实 AI 产品完成六次独立初评，并验证一个可录制的 V1→V2 人工闭环。
- 顺序：基线 → 案例调研 → 六次初评 → 录制与复检 → 失败路径 → 修复与报告。
- 最大风险：公开产品可能要求登录、无法稳定本地运行，或 DeepSeek 在复杂真实输入上无法保存结构化结果。
- 2026-08-06 任务 0：已完整阅读 README、AGENTS 和既有开发进度。
- 基线：typecheck、4 个单测、原型验证、依赖审计、生产构建、1 个 E2E 均通过。
- 环境事件：初次 E2E 被既有 `next dev` 开发锁阻止，停止该服务后重跑通过；本地产品已恢复至 http://localhost:3000。
- 案例：Customer Service Agents Demo（MIT，本地临时 UI）、OpenAgent（Apache-2.0，公开只读 Demo）、HuggingChat / Chat UI（Apache-2.0，公开首页）；来源和边界已记入 `docs/evaluation/cases.md`。
- 六次成功初评：客服、OpenAgent、HuggingChat 各两次；每次均保存 `deepseek-v4-pro` Run、资料检索、DOM 检查、Issue 与证据。Open Canvas（已下线）和 DocsGPT（仅 Spinner）作为淘汰候选保留真实失败证据，不计入六次。
- 代表闭环：已录制 Customer Service V1→人工处理中→基于真实项目的 V2→真实复检“疑似已解决”→产品经理关闭；视频与截图在 `.artifacts/overnight/`，时长 54.08 秒、1440×900、已实际播放验证。
- 失败路径：空资料、路径穿越 ZIP、不可访问 URL、缺少密钥、无效模型地址和刷新持久化已真实验证；新增 E2E 回归覆盖前五项和故障后恢复。
- 最终回归：typecheck、4 个单测、2 个端到端测试、生产构建、冻结原型验证和高等级依赖审计均通过；视频已实际连续播放抽样确认。
- 提交状态：已提交本轮“Validate real AI product review loops”成果；`.artifacts/`、SQLite 数据库和第三方源码均被 Git 忽略，密钥扫描通过。
- 发布：已确认 GitHub SSH 身份为 `Frenkie99`，并已通过 SSH 推送到 `origin/main`。

## 采集质量与重复评审比较

- 目标：识别不可评审页面，并让两个真实初评 Run 的共同、独有及证据波动可审计。
- 顺序：采集质量测试与门禁 → 确定性 Run 比较 → 稳定性视图 → P2 安全边界文档 → 全量验证与发布。
- 基线：`1e29860`；4 个单元测试、2 个 E2E、类型检查、构建、冻结原型校验和高等级依赖审计均通过。
- 最大风险：页面质量启发式误判，以及短标题中文分词不足导致主题错配；规则必须透明且保留人工确认。
- 范围：本轮不实现多页、登录态、Cookie 或凭据采集，只记录后续安全设计。
- 实现：采集质量门禁和 Run 比较已完成；HuggingChat 两次真实初评显示共同主题 1、仅 A 2、仅 B 0，并标记证据波动。
- 回归：12 个单元测试、3 个桌面 E2E、类型检查、生产构建、冻结原型校验与高等级依赖审计全部通过，跳过 0。
- 发布：密钥与暂存范围检查通过，已以 `Frenkie99` 署名通过 SSH 推送 `main`。

## 免费临时公网演示

- 目标：不购买域名或云服务器，使用当前 Mac 安全地提供预约式 ProtoAlign 公网演示。
- 实现：站点共享密码、Asia/Shanghai 自然日 Agent 额度、真实 SQLite Run 计数和公网 URL 内网地址阻断。
- 运行：`npm run share:demo` 自动生产构建、启动 `127.0.0.1:3010` 并创建 Cloudflare Quick Tunnel。
- 本机配置：随机访问密码只保存在 Git 忽略的 `.env.demo.local`；考虑今天已有 13 次真实验证 Run，本机今日额度设为 20 次，模板默认 12 次。
- 边界：电脑与网络必须在线，临时链接会变化；该方案不宣称正式用户鉴权或生产 SLA。
- 验证：公网与本地均为未授权 401、正确凭据 200；真实隔离服务确认内网 URL 400、缺少模型密钥 502、达到额度 429；35 个单元测试、3 个 E2E、类型检查、生产构建、原型校验和依赖审计通过。

## 面试自助交付收口

- 目标：让面试官无需讲解也能在五分钟内理解真实 Agent、证据链、人工边界与跨版本价值。
- 公开项目：仅展示 Customer Service Agents 完整闭环、OpenAgent 工具与隔离、HuggingChat 稳定性评审；保留本次公网进程中新建空白项目的真实试用入口。
- 案例丰富度：Customer Service 在既有已解决 V1→V2 Issue 基础上再次真实调用 DeepSeek，新增“Available Agents 空态”和“取消确认流程 / 人工边界”两条有资料与 DOM 证据的问题；公开层默认仍选中包含复检和人工决定的闭环 Issue。三个案例现分别有 3、3、4 条真实 Issue。
- 页面：公网模式首次打开显示五分钟导览；资料导入明确支持格式、单机持久化与“请勿上传机密资料”提示，本地模式不显示这些公网包装。
- 材料：README 增加一分钟摘要；演示脚本已按真实 Next.js MVP 重写，删除 Figma、音频转写和浏览器本地存储等错误声明；新增面试官交付信息模板。
- 录屏：现有 Customer Service V1→V2 WebM 已再次通过浏览器打开并显示真实导入画面；沿用既有 54.08 秒、1440×900 可播放性验证，不重录。
- 验证：36 个单元测试、3 个 E2E、类型检查、生产构建、冻结原型校验和高等级依赖审计通过；桌面浏览器完成导览、三案例顺序、活动记录、运行比较和输入警告检查；生产模式确认未认证 401、正确凭据 200、公开列表恰好 3 个项目。
- 部署衔接：本轮没有中断另一个任务正在运行的 3010 端口旧构建；发布代码后必须由该任务重启公网演示，已有页面才会加载精选列表与导览。
